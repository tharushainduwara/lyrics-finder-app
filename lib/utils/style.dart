import 'package:flutter/material.dart';

class Mainapppading {
  static const EdgeInsets main = EdgeInsets.all(30.0);
}

class Inputborderradius {
  static BorderRadius main = BorderRadius.circular(10.0);
}

class Containerborderradius {
  static BorderRadius main = BorderRadius.circular(20.0);
}

